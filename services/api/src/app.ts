import express,{type Express}from 'express';import helmet from 'helmet';import cors from 'cors';import {api}from './routes/api.js';import {internalTelemetry}from './realtime/internal-telemetry-route.js';import {errorHandler}from './lib/errors.js';import {env}from './config/env.js';import {pool}from './db/pool.js';import {apiRateLimit,authRateLimit,historyRateLimit}from './middleware/rate-limits.js';
export const app:Express=express();app.use(helmet());app.use(cors({origin:env.CORS_ORIGINS.split(','),credentials:true}));app.use(express.json({limit:'100kb'}));app.use('/internal/v1',internalTelemetry);app.use('/api/v1/auth',authRateLimit);app.use('/api/v1/vehicles/:id/history',historyRateLimit);app.use('/api/v1',apiRateLimit);app.get('/health',(_q,r)=>r.json({success:true,data:{status:'ok'}}));app.get('/health/live',(_q,r)=>r.json({success:true,data:{status:'live'}}));app.get('/health/ready',async(_q,r,next)=>{try{await pool.query('SELECT 1');r.json({success:true,data:{status:'ready'}})}catch(error){next(error)}});app.use('/api/v1',api);app.use(errorHandler);
