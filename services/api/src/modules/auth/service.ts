import bcrypt from 'bcrypt'; import crypto from 'node:crypto'; import jwt from 'jsonwebtoken'; import {query} from '../../db/pool.js'; import {env} from '../../config/env.js'; import {AppError} from '../../lib/errors.js'; import type {Role} from '@fleet/shared-types';
type User={id:string;email:string;password_hash:string;role:Role}; const hash=(x:string)=>crypto.createHash('sha256').update(x).digest('hex');
const issue=(u:User)=>({accessToken:jwt.sign({id:u.id,role:u.role},env.JWT_SECRET,{expiresIn:'15m'}),refreshToken:jwt.sign({id:u.id,role:u.role},env.JWT_REFRESH_SECRET,{expiresIn:'30d'})});
export async function login(email:string,password:string){const r=await query<User>('SELECT id,email,password_hash,role FROM users WHERE email=$1 AND active=true',[email.toLowerCase()]);const u=r.rows[0];if(!u||!await bcrypt.compare(password,u.password_hash))throw new AppError(401,'INVALID_CREDENTIALS','Invalid email or password');const tokens=issue(u);await query('INSERT INTO refresh_tokens(user_id,token_hash,expires_at) VALUES($1,$2,now()+interval \'30 days\')',[u.id,hash(tokens.refreshToken)]);return tokens}
export async function refresh(token:string){let claims:{id:string;role:Role};try{claims=jwt.verify(token,env.JWT_REFRESH_SECRET) as typeof claims}catch{throw new AppError(401,'INVALID_REFRESH_TOKEN','Invalid refresh token')}const r=await query<User>('SELECT u.id,u.email,u.password_hash,u.role FROM users u JOIN refresh_tokens t ON t.user_id=u.id WHERE t.token_hash=$1 AND t.revoked_at IS NULL AND t.expires_at>now()',[hash(token)]);if(!r.rows[0])throw new AppError(401,'INVALID_REFRESH_TOKEN','Invalid refresh token');await query('UPDATE refresh_tokens SET revoked_at=now() WHERE token_hash=$1',[hash(token)]);const tokens=issue(r.rows[0]);await query('INSERT INTO refresh_tokens(user_id,token_hash,expires_at) VALUES($1,$2,now()+interval \'30 days\')',[claims.id,hash(tokens.refreshToken)]);return tokens}
export const logout=(token:string)=>query('UPDATE refresh_tokens SET revoked_at=now() WHERE token_hash=$1',[hash(token)]);
