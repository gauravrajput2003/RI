import {describe,it,expect}from 'vitest';import {ConnectionManager}from '../src/transport/connection-manager.js';describe('connection contexts',()=>{it('isolates device and protocol state for simultaneous trackers',()=>{const manager=new ConnectionManager();const a=manager.create('10.0.0.1');const b=manager.create('10.0.0.2');const c=manager.create('10.0.0.3');a.context.protocol='W15';a.context.identityValue='device-a';b.context.protocol='W15';b.context.identityValue='device-b';c.context.protocol='GT06';c.context.identityValue='device-c';a.buffer.append(Buffer.from([1,2]));expect([a.context,b.context,c.context].map(context=>[context.protocol,context.identityValue])).toEqual([['W15','device-a'],['W15','device-b'],['GT06','device-c']]);expect(b.buffer.append(Buffer.from([3]))).toEqual(Buffer.from([3]))})});
