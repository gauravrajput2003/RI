import {describe,it,expect}from 'vitest';import {readFileSync}from 'node:fs';import {Gt06Decoder}from '../src/protocols/gt06/decoder.js';import {validGt06Frame}from '../src/protocols/gt06/parser.js';
const fixture=(name:string)=>Buffer.from(readFileSync(new URL(`./fixtures/gt06/${name}.hex`,import.meta.url),'utf8').trim(),'hex');const decoder=new Gt06Decoder();
describe('GT06 real captures',()=>{it('validates frame length and CRC then extracts login identity',()=>{const frame=fixture('login');expect(validGt06Frame(frame)).toBe(true);expect(decoder.decode(frame,new Date()).imei).toBe('864943046906604')});it('decodes heartbeat terminal status',()=>expect(decoder.decode(fixture('heartbeat'),new Date()).metadata).toMatchObject({terminalInfo:0x45,ignition:false,gpsPositioned:true,charging:true,defense:true,oilElectricityConnected:true,voltageLevel:6,gsmSignal:4}));it('normalizes the captured GPS data without inventing ignition',()=>{const location=decoder.decode(fixture('location'),new Date()).location!;expect(location.trackerTimestamp?.toISOString()).toBe('2006-12-28T16:42:14.000Z');expect(location.satellites).toBe(6);expect(location.latitude).toBeCloseTo(28.879542,5);expect(location.longitude).toBeCloseTo(76.586764,5);expect(location.speed).toBe(24);expect(location.course).toBe(166);expect(location.ignition).toBeNull()})});
