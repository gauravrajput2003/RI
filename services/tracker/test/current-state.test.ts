import {describe,it,expect}from 'vitest';import {heartbeatState}from '../src/services/current-state.js';describe('current-state semantics',()=>{it('sets current speed to zero only for known ignition off',()=>{expect(heartbeatState({ignition:false,gsmSignal:4})).toEqual({ignition:false,gsmSignal:4,speed:0,state:'STOPPED'});expect(heartbeatState({})).toEqual({ignition:null,gsmSignal:null,speed:null,state:'ONLINE'})})});
