import {describe,it,expect}from 'vitest';import {framingRegistry}from '../src/protocols/framing/framing-registry.js';describe('framing registry',()=>{it('selects a family without selecting GT06 or W15',()=>expect(framingRegistry.identify(Buffer.from([0x78,0x78,0x0a]))?.name).toBe('gt06-family'));it('leaves unsupported bytes unframed for diagnostics',()=>expect(framingRegistry.identify(Buffer.from([0xff,0x01]))).toBeUndefined())});
